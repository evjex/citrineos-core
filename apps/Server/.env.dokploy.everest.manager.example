# Everest manager app env (Dokploy split image mode)
# App image/build: manager (Dockerfile or ghcr manager image with start script)
OCPP_VERSION=two
EVEREST_TARGET_URL=wss://ocpp.rangoru.com/cp001
# Manager uses MQTT_SERVER_ADDRESS in split image mode.
MQTT_SERVER_ADDRESS=core-everest-mqtt
